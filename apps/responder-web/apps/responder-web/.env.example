# Only needed in local dev if apps/api runs on a different port/origin than
# this app. When set, /api/* requests from the browser are proxied there
# (see next.config.mjs). Leave unset if served behind a shared gateway.
RESCUE_LINK_API_ORIGIN=http://localhost:8000
